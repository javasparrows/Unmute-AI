services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: writing-paper-image
    container_name: writing-paper-container
    env_file: .env.local
    environment:
      NODE_ENV: production
      PORT: "3000"
    ports:
      - "3000:3000"
    restart: unless-stopped
